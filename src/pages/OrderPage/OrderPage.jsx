import React from 'react';
import Orders from '../../Components/Orders/Orders';

const OrderPage = () => {
    return (
        <div>
            <Orders/>
        </div>
    );
};

export default OrderPage;